# Safe Configuration
SAFE_CHAIN_ID=8453                  # Base = 8453, Ethereum = 1, Polygon = 137, etc.
SAFE_ADDRESS=0xYourSafeAddress       # Your Gnosis Safe address (checksummed)
SAFE_API_KEY=your-safe-api-key      # Optional: Get from https://developer.safe.global

# Telegram Configuration
TELEGRAM_BOT_TOKEN=123456:ABC...    # From @BotFather on Telegram
TELEGRAM_CHAT_ID=-1001234567890     # Your group/channel ID (see SETUP.md for instructions)

# Vercel KV Storage (optional - will use in-memory storage if not configured)
# These are automatically set when you connect Vercel KV to your project
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Cron Security (optional but recommended for production)
CRON_SECRET=your-secret-key-here    # Random string to secure the cron endpoint
