# Safe Configuration
SAFE_CHAIN_ID=8453                  # Base = 8453, Ethereum = 1, Polygon = 137, etc.
SAFE_ADDRESS=0xYourSafeAddress       # Your Gnosis Safe address (checksummed)
SAFE_API_KEY=your-safe-api-key      # Optional: Get from https://developer.safe.global

# Telegram Configuration
TELEGRAM_BOT_TOKEN=123456:ABC...    # From @BotFather on Telegram
TELEGRAM_CHAT_ID=-1001234567890     # Your group/channel ID (see SETUP.md for instructions)

# Vercel Edge Config (optional - will use in-memory storage if not configured)
# This is automatically set when you connect Vercel Edge Config to your project
EDGE_CONFIG=

# Cron Security (optional but recommended for production)
CRON_SECRET=your-secret-key-here    # Random string to secure the cron endpoint

# Thirdweb Configuration (for social profiles and avatars)
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=your-thirdweb-client-id    # Get from https://thirdweb.com/dashboard

# Zapper Configuration (for human-readable names fallback)
ZAPPER_API_KEY=your-zapper-api-key            # Get from https://zapper.xyz/api

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000     # Your app's public URL for OG images
